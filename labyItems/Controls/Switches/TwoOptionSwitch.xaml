<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="labyItems.Controls.TwoOptionSwitch"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Name="Root">

    <!-- <Grid ColumnDefinitions="Auto,Auto,Auto" ColumnSpacing="8">
        <Label Grid.Column="0"
               VerticalOptions="Center"
               Text="{Binding LeftText, Source={x:Reference Root}}" />-->

        <!-- Grid.Column="1" -->
    <Switch
        x:Name="InnerSwitch"
        Toggled="InnerSwitch_OnToggled"
        VerticalOptions="Center" />

    <!-- Right label
        <Label Grid.Column="2"
               VerticalOptions="Center"
               Text="{Binding RightText, Source={x:Reference Root}}" />
    </Grid>-->
</ContentView>
