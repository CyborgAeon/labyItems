<?xml version="1.0" encoding="utf-8" ?>
<controls:EnumPicker x:Class="labyItems.Controls.MagicColourPicker"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:enums="clr-namespace:labyItems.Models.Enums"
    xmlns:controls="clr-namespace:labyItems.Controls" x:Name="Root" x:TypeArguments="enums:MagicColours">
    <!-- <ContentPage.BindingContext>
        <viewmodels:SearchViewModel />
    </ContentPage.BindingContext>
    <StackLayout>
        <SearchBar x:Name="searchBar" SearchCommand="{Binding PerformSearch}" SearchCommandParameter="{Binding Text, x:DataType='SearchBar', Source={x:Reference searchBar}}"/>
        <ListView x:Name="searchResults" ItemsSource="{Binding SearchResults}" />
    </StackLayout> -->

    <Grid x:Name="RootGrid" IsClippedToBounds="False">
        <VerticalStackLayout x:Name="Container" Spacing="10">
            <VerticalStackLayout.Triggers>
                <DataTrigger TargetType="VerticalStackLayout" Binding="{Binding Source={x:Reference Root}, Path=IsCompact}" Value="True">
                    <Setter Property="Spacing" Value="2"/>
                    <Setter Property="VerticalOptions" Value="Center"/>
                </DataTrigger>
            </VerticalStackLayout.Triggers>

            <Label x:Name="TitleLabel" FontAttributes="Bold" VerticalOptions="Center" Text="{Binding LabelText, Source={x:Reference Root}}" TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource White}}"/>

            <Border x:Name="InputBorder" Padding="12,10" Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray600}}" StrokeThickness="1" Background="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource OffBlack}}">
                <Border.Triggers>
                    <DataTrigger TargetType="Border" Binding="{Binding Source={x:Reference Root}, Path=IsCompact}" Value="True">
                        <Setter Property="StrokeThickness" Value="0"/>
                        <Setter Property="Background" Value="White"/>
                        <Setter Property="Padding" Value="0"/>
                    </DataTrigger>
                </Border.Triggers>
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="12"/>
                </Border.StrokeShape>
                <Entry Grid.Column="0" x:Name="SearchEntry" BackgroundColor="White" Placeholder="{Binding PlaceholderText, Source={x:Reference Root}}" TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource White}}" PlaceholderColor="{AppThemeBinding Light={StaticResource Gray400}, Dark={StaticResource Gray500}}" Margin="0"/>
            </Border>
        </VerticalStackLayout>

        <AbsoluteLayout IsVisible="{Binding AreSuggestionsVisible, Source={x:Reference Root}}" InputTransparent="False" IsClippedToBounds="False" HeightRequest="0" VerticalOptions="Start" HorizontalOptions="FillAndExpand" ZIndex="200" AbsoluteLayout.LayoutFlags="All" AbsoluteLayout.LayoutBounds="0,0,1,1">
            <BoxView Background="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource OffBlack}}" Opacity="1" AbsoluteLayout.LayoutBounds="0,0,1,1" AbsoluteLayout.LayoutFlags="All">
                <BoxView.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnOverlayTapped"/>
                </BoxView.GestureRecognizers>
            </BoxView>
            <Border Margin="0,6,0,0" Padding="4" StrokeThickness="1" Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray600}}" Background="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource OffBlack}}" Opacity="1" AbsoluteLayout.LayoutBounds="0,0,1,220" AbsoluteLayout.LayoutFlags="WidthProportional" TranslationY="{Binding Source={x:Reference InputBorder}, Path=Height}">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="12"/>
                </Border.StrokeShape>
                <Border.Shadow>
                    <Shadow Brush="{StaticResource PrimaryBrush}" Radius="10" Offset="0,2"/>
                </Border.Shadow>

                <CollectionView x:Name="SuggestionsView" SelectionMode="Single" BackgroundColor="White">
                    <CollectionView.ItemsLayout>
                        <LinearItemsLayout Orientation="Vertical" ItemSpacing="6"/>
                    </CollectionView.ItemsLayout>
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="12,10" Stroke="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray600}}" StrokeThickness="1" Background="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource OffBlack}}">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="10"/>
                                </Border.StrokeShape>
                                <Grid ColumnDefinitions="Auto,*" ColumnSpacing="8">
                                    <BoxView WidthRequest="10" HeightRequest="10" VerticalOptions="Center" CornerRadius="3" Color="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource Primary}}" BackgroundColor="White"/>
                                    <Label VerticalOptions="Center" TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource White}}" Text="{Binding ., Converter={StaticResource DisplayConverter}}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </Border>
        </AbsoluteLayout>
    </Grid>
</controls:EnumPicker>
